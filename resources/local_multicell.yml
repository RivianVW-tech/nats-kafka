services:
  zookeeper:
    # Only Kafka talks to Zookeeper, over the compose network; no host port
    # is published so a busy local 2181 cannot break the environment.
    image: wurstmeister/zookeeper
  kafka:
    image: wurstmeister/kafka
    # Host port 9192 keeps this environment clear of the integration-test
    # servers (resources/test_servers.yml) and anything else on 9092.
    ports:
      - "9192:9192"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: 'true'
      # pre-create the bridge output topic with 4 partitions; auto-create
      # would otherwise make it with 1 on first produce
      KAFKA_CREATE_TOPICS: "brand.telemetry:4:1"
      KAFKA_ADVERTISED_HOST_NAME: localhost
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENERS: PLAINTEXT://:9192
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9192
  nats-cell-a:
    image: nats:2.10-alpine
    command: ["-js", "-m", "8222", "--server_name", "cell-a"]
    ports:
      - "4222:4222"
      - "8222:8222"
  nats-cell-b:
    image: nats:2.10-alpine
    command: ["-js", "-m", "8222", "--server_name", "cell-b"]
    ports:
      - "4223:4222"
      - "8223:8222"
